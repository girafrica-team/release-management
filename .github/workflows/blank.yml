name: Checkout Repository
on:
    workflow_dispatch:
    push:
        branches:
          - main
jobs:
    checkout:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout repository
          uses: actions/checkout@v2

    print_text:
        environment: Production
        runs-on: ubuntu-latest

        steps:
        - name: Print TEXT
          run: ${{ env.test }}
              
    print_text2:
        runs-on: ubuntu-latest

        steps:
        - name: Print TEXT2
          run: echo "TEXT2"
